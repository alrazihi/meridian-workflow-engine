server:
  port: 8080

spring:
  application:
    name: meridian-workflow-engine
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/meridian}
    username: ${DATABASE_USERNAME:meridian}
    password: ${DATABASE_PASSWORD:meridian}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  kafka:
    bootstrap-servers: localhost:9092
    topics:
      document-events: document.events
      workflow-tasks: workflow.tasks
      workflow-completed: workflow.completed
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      enable-idempotency: true
      transaction-id-prefix: meridian-tx
    consumer:
      group-id: meridian-workflow-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    com.meridian: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
